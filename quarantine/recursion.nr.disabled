// ARCHIVE ONLY: this old API is not exported or compiled.
use dep::std;

pub fn verify_nested_proof<let N: u32>(
    verification_key: [Field; 114],
    proof: [Field; 93],
    public_inputs: [Field; N],
    key_hash: Field,
) {
    std::verify_proof(
        verification_key.as_slice(),
        proof.as_slice(),
        public_inputs.as_slice(),
        key_hash,
    );
}
